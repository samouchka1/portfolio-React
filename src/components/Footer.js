import React, { useState } from 'react';
import {
    Box,
    Button,
    Typography,
    Container
} from '@mui/material';



const Footer = () => {

    return (
        <Container maxWidth={false}>
        </Container>
    )
}